const tabs={status:{title:'Status',body:'Mostra versão do Hermes, HERMES_HOME, config_path, env_path, gateway, plataformas e sessões ativas. Use primeiro para saber se o sistema está saudável.'},config:{title:'Config',body:'Editor visual do config.yaml. Alterações críticas podem exigir nova sessão ou restart do gateway.'},env:{title:'API Keys / Env',body:'Gerencia variáveis do .env. Trate esta área como sensível: tokens e chaves aparecem redigidos, mas podem ser modificados.'},sessions:{title:'Sessões',body:'Audite conversas, sessões Telegram/CLI/cron e tool calls para entender o que o agente fez.'},logs:{title:'Logs',body:'Local para diagnosticar falhas de gateway, API, cron, browser e integrações.'},cron:{title:'Cron Jobs',body:'Crie, pause, retome e dispare automações recorrentes como auditorias noturnas.'},skills:{title:'Skills / Toolsets',body:'Veja capacidades carregadas, dependências e toolsets disponíveis para o Hermes Agent.'},ssh:{title:'SSH Tunnel / Hostinger Docker',body:'Roteiro seguro para acessar o dashboard em container Docker na Hostinger usando Docker mapping + túnel SSH, sem abrir a porta 9119 publicamente.',link:'#ssh-tunnel'}};const panel=document.getElementById('tabPanel');function renderTab(key){const item=tabs[key];panel.innerHTML=`<h3>${item.title}</h3><p>${item.body}</p>${item.link?`<p><a href="${item.link}">Abrir o procedimento completo →</a></p>`:''}`;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===key));}document.querySelectorAll('.tab').forEach(btn=>btn.addEventListener('click',()=>renderTab(btn.dataset.tab)));renderTab('status');document.querySelectorAll('.copy-button').forEach(btn=>btn.addEventListener('click',async()=>{const el=document.getElementById(btn.dataset.copy);try{await navigator.clipboard.writeText(el.innerText)}catch(e){const range=document.createRange();range.selectNodeContents(el);const sel=window.getSelection();sel.removeAllRanges();sel.addRange(range);document.execCommand('copy');sel.removeAllRanges()}const old=btn.textContent;btn.textContent='Copiado';setTimeout(()=>btn.textContent=old,1200)}));document.getElementById('themeToggle').addEventListener('click',()=>document.body.classList.toggle('dark-mode'));
